services:
  - type: web
    name: caffeine-store
    runtime: node
    buildCommand: ./render-build.sh
    startCommand: node server.js
    env:
      - key: MONGODB_URI
        value: mongodb+srv://username:password@cluster.mongodb.net/caffeine-store
